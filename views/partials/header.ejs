<header class="main-header">
  <div class="header-content">
    <h1><a href="/">My Blog</a></h1>

    <nav>
      <% if (currentUser) { %>
        <span class="welcome-text">
          Welcome, <strong><%= currentName %></strong>
        </span>
        <a href="/">Home</a>
        <a href="/logout">Logout</a>
      <% } else { %>
        <a href="/">Home</a>
        <a href="/signin">Sign In</a>
        <a href="/signup">Sign Up</a>
      <% } %>
    </nav>
  </div>
</header>
